<% provide(:title, 'All Users') %>

<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
	<%= render @users %>
	<!-- rails infers that @users is a list of User objects and iterates through them and renders _user.html.erb -->
</ul>

<!-- <ul class="users">
	<% @users.each do |user| %>
		<%= render user%>
	<% end %>
</ul> -->

<%= will_paginate %>
